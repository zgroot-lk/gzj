# 学生管理系统 - 详细运行步骤

## 📋 前置要求

1. **Java** >= 17（已安装 ✓）
2. **Maven** >= 3.6（已安装 ✓）
3. **MySQL** >= 8.0（需要确保已安装并运行）
4. **Node.js** >= 14.0.0（用于前端）

---

## 🚀 第一步：检查环境

### 1.1 检查 Java 版本
```bash
java -version
```
应该显示 Java 17 或更高版本。

### 1.2 检查 Maven 版本
```bash
mvn -version
```
应该显示 Maven 3.6 或更高版本。

### 1.3 检查 MySQL 是否运行
```bash
# macOS
mysql.server status

# 或者尝试连接
mysql -u root -p
```
如果能连接，说明 MySQL 正在运行。

---

## 🗄️ 第二步：配置数据库

### 2.1 创建数据库

打开终端，执行：
```bash
mysql -u root -p
```

输入 MySQL root 密码后，执行：
```sql
CREATE DATABASE student_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

### 2.2 导入数据库表结构

在项目根目录执行：
```bash
mysql -u root -p student_management < database/schema.sql
```

### 2.3 导入测试数据（可选）

```bash
mysql -u root -p student_management < database/init.sql
```

### 2.4 配置数据库连接

编辑文件：`src/main/resources/application.yml`

找到这一行：
```yaml
password:   # 这里填写你的MySQL root密码
```

**示例**（如果密码是 `123456`）：
```yaml
password: 123456
```

**如果 MySQL root 没有密码**，保持为空：
```yaml
password:
```

---

## ☕ 第三步：启动后端（Java Spring Boot）

### 方式一：使用 IntelliJ IDEA（推荐）

1. 打开 IntelliJ IDEA
2. 打开项目：`文件` → `打开` → 选择项目目录
3. 等待 Maven 自动下载依赖（右下角会显示进度）
4. 找到 `src/main/java/com/student/StudentManagementApplication.java`
5. 右键点击 → `运行 'StudentManagementApplication'`

### 方式二：使用 Maven 命令行

在项目根目录执行：
```bash
mvn spring-boot:run
```

### 验证后端启动成功

看到以下日志说明启动成功：
```
Tomcat started on port(s): 3001 (http)
Started StudentManagementApplication in X.XXX seconds
```

**测试后端API**：
打开浏览器访问：http://localhost:3001/api/students

应该返回 JSON 数据（可能是空数组 `[]`）。

---

## ⚛️ 第四步：启动前端（React）

### 4.1 安装前端依赖（首次运行）

在项目根目录执行：
```bash
cd client
npm install
```

**注意**：如果 `npm install` 很慢或失败，可以使用国内镜像：
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### 4.2 启动前端服务

在 `client` 目录下执行：
```bash
npm start
```

等待编译完成，会自动打开浏览器访问：http://localhost:3000

---

## ✅ 第五步：验证系统运行

### 5.1 检查后端
- 访问：http://localhost:3001/api/students
- 应该返回 JSON 数据

### 5.2 检查前端
- 访问：http://localhost:3000
- 应该看到系统首页

### 5.3 测试功能
1. 点击左侧菜单的"学生管理"
2. 点击"新增学生"按钮
3. 填写表单并提交
4. 应该能看到新添加的学生

---

## 🔧 常见问题解决

### 问题1：后端启动失败 - 数据库连接错误

**错误信息**：`Access denied for user 'root'@'localhost'`

**解决方法**：
1. 检查 `application.yml` 中的密码是否正确
2. 确认 MySQL root 用户密码
3. 如果忘记密码，可以重置 MySQL root 密码

### 问题2：后端启动失败 - 端口被占用

**错误信息**：`Port 3001 is already in use`

**解决方法**：
```bash
# 查找占用端口的进程
lsof -i :3001

# 杀死进程（替换 PID 为实际进程号）
kill -9 PID
```

或者修改 `application.yml` 中的端口：
```yaml
server:
  port: 3002  # 改为其他端口
```

### 问题3：前端启动失败 - 端口被占用

**错误信息**：`Something is already running on port 3000`

**解决方法**：
```bash
# 查找占用端口的进程
lsof -i :3000

# 杀死进程
kill -9 PID
```

### 问题4：前端无法连接后端

**解决方法**：
1. 确认后端已启动（访问 http://localhost:3001/api/students）
2. 检查浏览器控制台（F12）的错误信息
3. 确认后端端口是 3001

### 问题5：Maven 下载依赖很慢

**解决方法**：
配置 Maven 使用国内镜像，编辑 `~/.m2/settings.xml`：
```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

---

## 📝 完整启动命令总结

### 第一次运行（需要安装依赖）

```bash
# 1. 进入项目目录
cd "/Users/zhangliangkai/Documents/项目/gzj学生管理系统"

# 2. 配置数据库（在MySQL中执行）
mysql -u root -p
# 然后执行：CREATE DATABASE student_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
# 然后执行：EXIT;

# 3. 导入数据库表结构
mysql -u root -p student_management < database/schema.sql

# 4. 导入测试数据（可选）
mysql -u root -p student_management < database/init.sql

# 5. 配置 application.yml 中的数据库密码

# 6. 启动后端（在 IntelliJ IDEA 中运行，或执行 mvn spring-boot:run）

# 7. 安装前端依赖（新开一个终端）
cd client
npm install

# 8. 启动前端
npm start
```

### 日常运行（依赖已安装）

```bash
# 终端1：启动后端
# 在 IntelliJ IDEA 中运行，或执行：
mvn spring-boot:run

# 终端2：启动前端
cd client
npm start
```

---

## 🎯 访问地址

- **前端界面**：http://localhost:3000
- **后端API**：http://localhost:3001/api
- **学生列表API**：http://localhost:3001/api/students
- **班级列表API**：http://localhost:3001/api/classes

---

## 💡 提示

1. **后端和前端需要同时运行**才能正常使用系统
2. **先启动后端，再启动前端**
3. 如果修改了代码，后端需要重启，前端会自动热更新
4. 关闭服务：在终端按 `Ctrl + C`

---

祝使用愉快！🎉

